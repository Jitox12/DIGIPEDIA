spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/digipedia
    username: postgres
    password: 12345
  jpa:
    hibernate:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  port: 8080